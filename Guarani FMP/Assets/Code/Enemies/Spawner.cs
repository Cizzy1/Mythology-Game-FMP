using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spawner : MonoBehaviour
{
    /* public GameObject EnemyPref;
    bool hasSpawned = false;
    
    void Update()
    {
        //hasSpawned = true;

        if(this.transform.childCount == 0){
            hasSpawned = false;
            if(!hasSpawned){
                Debug.Log("Enemy spawned");
                Spawn();
                hasSpawned = true;
            }
        }

    }

    void Spawn(){
        Instantiate(EnemyPref, this.transform.position, Quaternion.identity);
        hasSpawned = true;
    } */

    //-------------------------------
    //NOT WORKING
    //-------------------------------

}
